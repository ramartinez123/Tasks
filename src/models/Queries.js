import Task from '../models/Task'

/*function  queryGeneral() {
    const tasksa = Task.find().lean();
    return tasksa ;
};

function queryAdd(addT) {
    const xaddT = addT.save();
    return 0;
};

function queryDelete(id){
    Task.findByIdAndDelete(id);
}
module.exports ={queryGeneral,queryAdd,queryDelete};*/

